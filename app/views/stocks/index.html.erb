<p id="notice"><%= notice %></p>

<h1>Stocks</h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th class="text-right">Price (USD)</th>
    </tr>
  </thead>

  <tbody>
    <% @stocks.each do |stock| %>
      <tr>
        <td><%= stock.name %></td>
        <td class="text-right"><%= '%.2f' % stock.price %></td>
      </tr>
    <% end %>
  </tbody>
</table>
