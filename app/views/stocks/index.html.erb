<p id="notice"><%= notice %></p>

<h1>Stocks</h1>

<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th>Price</th>
      <th colspan="2"></th>
    </tr>
  </thead>

  <tbody>
    <% @stocks.each do |stock| %>
      <tr>
        <td><%= stock.name %></td>
        <td><%= stock.price %></td>
        <td><%= link_to 'Last month', stock %></td>
        <td><%= link_to 'Last year', stock %></td>
      </tr>
    <% end %>
  </tbody>
</table>
